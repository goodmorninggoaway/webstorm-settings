const React = require('react');
const $NAME = require('./$NAME').default;

describe('$NAME', () => {
    it('should do it', () => {
        const actual = ;
        expect(actual).not.to.be.undefined();
    });
});